# Git共同作業 完全ガイド

このガイドは、Gitの基礎から実践的なチーム開発までを網羅した学習教材です。

---

## 目次

1. [Gitの4つのエリア](#gitの4つのエリア)
2. [初回セットアップ：リモートからクローン](#初回セットアップリモートからクローン)
3. [ブランチ戦略](#ブランチ戦略)
4. [プルリクエスト（PR）](#プルリクエストpr)
5. [コードレビュー対応](#コードレビュー対応)
6. [git pull - 他の人の変更を取得](#git-pull---他の人の変更を取得)
7. [コンフリクト解決](#コンフリクト解決)
8. [チーム開発の1日の流れ](#チーム開発の1日の流れ)
9. [コマンドリファレンス](#コマンドリファレンス)

---

## Gitの4つのエリア

Gitでは、ファイルが以下の4つの場所を移動していきます：

```
①ワークツリー → ②ステージング → ③ローカルリポジトリ → ④リモートリポジトリ
 (作業場所)      (準備エリア)     (自分のPC)          (GitHub)
```

### たとえ話で理解する

- **①ワークツリー** = あなたの机の上（書類を作成・編集する場所）
- **②ステージングエリア** = 封筒に入れる前の書類置き場（送るものを選ぶ）
- **③ローカルリポジトリ** = あなたのファイリングキャビネット（記録として保管）
- **④リモートリポジトリ** = 会社の中央書庫（みんなが見られる場所）

### データの流れ

```
編集 → 追加 → コミット → プッシュ
(机)  (準備) (保管)   (共有)
  ↓      ↓      ↓       ↓
作業   選別   記録    公開
```

---

## 初回セットアップ：リモートからクローン

### シナリオ

あなたがチームに新しく参加し、既存のプロジェクトをローカルに取得する場合。

### ステップ1: GitHubにリポジトリが存在する

プロジェクトリーダー（田中さん）が既にGitHubにリポジトリを作成済み。

```
[GitHub（リモート）]
└─ project-name
   ├─ README.md
   ├─ index.html
   └─ style.css
   📦 コミット abc1234
```

### ステップ2: ローカルにクローン

```bash
# プロジェクトをローカルにコピー
git clone https://github.com/username/project-name.git
```

**実行結果：**
```
Cloning into 'project-name'...
```

### ステップ3: クローン後の状態

```
[あなたのPC（ローカル）]
└─ project-name/
   ├─ .git/           ← Git履歴も含めて完全コピー
   ├─ README.md       ← 同じ内容
   ├─ index.html      ← 同じ内容
   └─ style.css       ← 同じ内容
   📦 コミット abc1234 ← 履歴も同じ
```

### ステップ4: リモート設定の確認

```bash
git remote -v
```

**出力：**
```
origin  https://github.com/username/project-name.git (fetch)
origin  https://github.com/username/project-name.git (push)
```

### クローンとは何か？

- **完全なコピー**: ファイルだけでなく、全ての履歴も含む
- **初回のみ実行**: 2回目以降は `git pull` を使用
- **リモート設定**: `origin` として自動設定される

---

## ブランチ戦略

### なぜブランチが必要？

**問題：** masterブランチで直接作業すると...

```
あなたが作業中 → まだ完成していない
              ↓
        誤ってpush
              ↓
        本番環境が壊れる！
```

**解決策：** 作業用のブランチを作る

### ブランチの作成と切り替え

```bash
# 新しいブランチを作成して切り替え
git checkout -b feature/add-contact-form

# 確認
git branch
```

**出力：**
```
* feature/add-contact-form  ← 今ここ（*マーク）
  master
```

### ブランチの状態

```
master ────────────────┐
                       │ ← ここから分岐
feature/add-contact-form  ← 今ここで作業中
```

### ブランチで開発

```bash
# ファイルを編集
# contact.html を作成
# contact.js を作成

# 変更をステージング
git add contact.html contact.js

# コミット
git commit -m "feat: Add contact form feature"
```

### ブランチをリモートにプッシュ

```bash
git push origin feature/add-contact-form
```

**重要：** masterには影響しない！

```
master ───────────────────  ← 変更なし
                       │
feature/add-contact-form ─ 📦 新しいコミット
```

### ブランチ命名規則

| プレフィックス | 用途 | 例 |
|--------------|------|-----|
| `feature/` | 新機能開発 | `feature/user-login` |
| `fix/` | バグ修正 | `fix/header-alignment` |
| `docs/` | ドキュメント更新 | `docs/api-reference` |
| `refactor/` | リファクタリング | `refactor/database-layer` |

---

## プルリクエスト（PR）

### プルリクエストとは？

**定義：** 「この変更をmasterブランチに取り込んでください」というお願い

### メリット

- ✅ チームメンバーがコードレビューできる
- ✅ テストを自動実行できる
- ✅ masterに直接変更が入らない（安全）

### PRの作成

```bash
gh pr create \
  --title "Add contact form feature" \
  --body "## 概要
お問い合わせフォーム機能を追加しました。

## 変更内容
- contact.html: フォームのUI作成
- contact.js: バリデーションと送信処理

## テスト方法
1. contact.htmlをブラウザで開く
2. フォームに入力して送信
3. アラートが表示されることを確認

## レビュー依頼
@田中さん レビューお願いします！"
```

**結果：**
```
https://github.com/username/project-name/pull/1
```

### PRの状態

```
[GitHub]

Pull Request #1
┌─────────────────────────────────────┐
│ feature/add-contact-form            │
│          ↓                          │
│        master                       │
│                                     │
│ 状態: Open (レビュー待ち)            │
│ Files changed: +48 -0               │
│ Commits: 1                          │
└─────────────────────────────────────┘
```

---

## コードレビュー対応

### シナリオ

田中さんからレビューコメント：
> 「フォームにスタイルを追加してください」

### レビュー指摘を修正

```bash
# style.css を編集
# フォームのスタイルを追加

# 変更をコミット（同じブランチに追加）
git add style.css
git commit -m "style: Add styling for contact form

- Added form container styles
- Styled input fields and textarea
- Added hover effects for submit button

Addresses review feedback from 田中さん"

# プッシュ（PRが自動更新される）
git push origin feature/add-contact-form
```

### PRの更新

```
Pull Request #1
├─ 📦 abc1234 (初回コミット)
└─ 📦 def5678 (レビュー対応) ← 追加された
```

**重要：** 新しいPRを作る必要はない！同じブランチにプッシュすれば自動更新される。

---

## PRのマージ

### レビュー承認後

```bash
# PRをマージ（squashでコミットをまとめる）
gh pr merge 1 --squash --delete-branch
```

**実行結果：**
```
✓ Merged PR #1
✓ Deleted branch feature/add-contact-form
```

### マージ後の状態

```
master ──────┬─────────── 📦 マージコミット
             │
feature/     └─ 📦 def5678
add-contact    └─ 📦 abc1234
form          （削除済み）
```

### ローカルのmasterを更新

```bash
# masterブランチに切り替え
git checkout master

# 最新を取得
git pull origin master
```

---

## git pull - 他の人の変更を取得

### シナリオ

田中さんがmasterを更新した。あなたのローカルは古い状態。

```
[あなたのPC（ローカル）]
master: 📦 abc1234 ← 古い

[GitHub（リモート）]
master: 📦 xyz9999 ← 田中さんが更新した
```

### 最新を取得

```bash
# masterに切り替え
git checkout master

# 最新を取得してマージ
git pull origin master
```

**実行結果：**
```
Updating abc1234..xyz9999
Fast-forward
 CHANGELOG.md | 20 ++++++++++++++++++++
 1 file changed, 20 insertions(+)
```

### pull後の状態

```
[あなたのPC（ローカル）]
master: 📦 xyz9999 ← 最新！

[GitHub（リモート）]
master: 📦 xyz9999
```

### pullとfetchの違い

| コマンド | 動作 |
|---------|------|
| `git pull` | 取得 + マージ（自動） |
| `git fetch` | 取得のみ（マージは手動） |

### 作業前のルール

**必ず最新を取得してから作業開始！**

```bash
# 正しい手順
git checkout master
git pull origin master              # ← 最新を取得
git checkout -b feature/new-feature # ← 最新から分岐
```

---

## コンフリクト解決

### コンフリクトが発生する状況

```
あなた: README.md の12行目を編集
田中さん: README.md の12行目を編集
        ↓
    コンフリクト！
```

### ステップ1: コンフリクトの検出

```bash
git merge master
```

**エラー出力：**
```
Auto-merging README.md
CONFLICT (content): Merge conflict in README.md
Automatic merge failed; fix conflicts and then commit the result.
```

### ステップ2: 状態確認

```bash
git status
```

**出力：**
```
Unmerged paths:
  both modified:   README.md
```

### ステップ3: ファイルを開く

```markdown
## チームメンバー

<<<<<<< HEAD
- 田中さん（先輩エンジニア）- プロジェクト作成者
- 小島さん（新メンバー）- フロントエンド担当
=======
- 田中太郎（シニアエンジニア）- プロジェクトリード
- 新メンバー募集中
>>>>>>> master
```

### コンフリクトマーカーの意味

```
<<<<<<< HEAD        ← あなたの変更（現在のブランチ）
あなたの変更内容
=======            ← 区切り線
相手の変更内容
>>>>>>> master      ← masterの変更
```

### ステップ4: 正しい内容に編集

両方の情報を統合します：

```markdown
## チームメンバー

- 田中太郎（シニアエンジニア）- プロジェクトリード
- 小島さん（フロントエンドエンジニア）- UI/UX担当
```

**重要：** マーカー（`<<<<<<<`, `=======`, `>>>>>>>`）は削除する！

### ステップ5: 解決をコミット

```bash
# 解決したファイルを追加
git add README.md

# マージコミット
git commit -m "Merge branch 'master' into feature/update-readme

Resolved conflicts in README.md:
- Combined team member information from both branches"
```

### 解決後の履歴

```
*   📦 マージコミット (コンフリクト解決)
|\
| * 📦 master (田中さんの変更)
* | 📦 feature (あなたの変更)
|/
* 📦 共通の祖先
```

---

## チーム開発の1日の流れ

### 朝（作業開始時）

```bash
# 1. 最新を取得
git checkout master
git pull origin master

# 2. 新しいブランチで作業開始
git checkout -b feature/new-feature
```

### 日中（開発中）

```bash
# 3. ファイル編集
# （エディタで作業）

# 4. 定期的にコミット
git add .
git commit -m "Add user authentication logic"

# 5. こまめにプッシュ（バックアップ）
git push origin feature/new-feature
```

### 夕方（作業完了時）

```bash
# 6. PRを作成
gh pr create --title "Add user authentication"

# 7. レビュー待ち
# （翌日、レビューコメントに対応）
```

### レビュー対応

```bash
# 8. 修正
git add .
git commit -m "Fix validation logic per review"
git push origin feature/new-feature
# → PRが自動更新される
```

### マージ後

```bash
# 9. masterに戻って最新を取得
git checkout master
git pull origin master

# 10. 次の機能へ
git checkout -b feature/next-feature
```

---

## コマンドリファレンス

### 基本操作

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `git clone <URL>` | リモートからコピー | 初回のみ |
| `git status` | 状態確認 | 常時 |
| `git log --graph --oneline` | 履歴を視覚的に表示 | 確認時 |

### ブランチ操作

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `git branch` | ブランチ一覧 | 確認時 |
| `git checkout -b <branch>` | ブランチ作成＆切り替え | 新機能開始時 |
| `git checkout <branch>` | ブランチ切り替え | ブランチ移動時 |
| `git branch -d <branch>` | ブランチ削除 | マージ後 |

### 変更の記録

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `git add <file>` | ファイルをステージング | 変更後 |
| `git add .` | 全ての変更をステージング | まとめて追加時 |
| `git commit -m "message"` | コミット | 区切りのよいとき |
| `git commit --amend` | 直前のコミットを修正 | コミット直後の訂正 |

### リモート操作

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `git push origin <branch>` | リモートに送信 | PR作成前 |
| `git pull origin <branch>` | 最新を取得＆マージ | 作業開始前 |
| `git fetch` | 取得のみ | 確認だけしたいとき |
| `git remote -v` | リモート設定確認 | 確認時 |

### マージとコンフリクト

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `git merge <branch>` | ブランチをマージ | ブランチ統合時 |
| `git merge --abort` | マージを中止 | コンフリクト解決を諦める時 |
| `git diff` | 変更内容を表示 | コンフリクト確認時 |

### GitHub CLI (gh)

| コマンド | 説明 | 使用タイミング |
|---------|------|--------------|
| `gh pr create` | PRを作成 | 開発完了時 |
| `gh pr list` | PR一覧 | 確認時 |
| `gh pr view <number>` | PR詳細表示 | レビュー時 |
| `gh pr merge <number>` | PRをマージ | 承認後 |

---

## よくあるシナリオと対処法

### 1. 間違えてmasterで作業してしまった

```bash
# 現在の変更を新しいブランチに移動
git checkout -b feature/correct-branch

# masterを元に戻す
git checkout master
git reset --hard origin/master
```

### 2. コミットメッセージを間違えた

```bash
# 直前のコミットメッセージを修正
git commit --amend -m "正しいメッセージ"

# まだpushしていない場合のみ安全
```

### 3. プッシュ済みのコミットを取り消したい

```bash
# ⚠️ 危険な操作！チームに確認してから

# 1つ前に戻す
git revert HEAD

# pushする
git push origin <branch>
```

### 4. 作業を一時保存したい

```bash
# 作業を退避
git stash

# 別の作業...

# 作業を復元
git stash pop
```

### 5. ブランチが最新のmasterから分岐していない

```bash
# 現在のブランチで
git fetch origin
git rebase origin/master

# コンフリクトがあれば解決
git add <file>
git rebase --continue
```

---

## ベストプラクティス

### コミットメッセージ

**良い例：**
```
feat: Add user authentication feature

- Implemented JWT token generation
- Added login/logout endpoints
- Created middleware for protected routes
```

**悪い例：**
```
update
fix
変更
```

### コミットの粒度

**良い例：**
- 1つの機能 = 1コミット
- 意味のある単位でコミット

**悪い例：**
- 1日分をまとめて1コミット
- 無関係な変更を1つのコミットに

### ブランチの寿命

- ✅ 短命（数日～1週間）
- ❌ 長期間（1ヶ月以上）

**理由：** 長く分岐すると、masterとの差分が大きくなりコンフリクトが増える

### プルリクエストのサイズ

- ✅ 小さく（200-400行程度）
- ❌ 巨大（1000行以上）

**理由：** レビューしやすく、問題の特定が容易

---

## トラブルシューティング

### Q: "Your branch is behind 'origin/master'"と表示される

**A:** リモートの方が新しい

```bash
git pull origin master
```

### Q: "Your branch is ahead of 'origin/master'"と表示される

**A:** ローカルの方が新しい

```bash
git push origin master
```

### Q: コンフリクトが怖い

**A:** 対処法

1. こまめに `git pull` する
2. 大きな変更は避ける
3. コンフリクトは「両方の変更を統合」すればOK

### Q: 間違えてコミットした

**A:** まだpushしていなければ

```bash
# 直前のコミットを取り消し（変更は残る）
git reset --soft HEAD^

# やり直す
git add <correct-files>
git commit -m "正しいメッセージ"
```

### Q: ブランチを削除してしまった

**A:** ローカルなら復元可能

```bash
# 削除されたブランチのコミットIDを探す
git reflog

# ブランチを復元
git checkout -b <branch-name> <commit-id>
```

---

## 学習リソース

### 公式ドキュメント

- [Git公式ドキュメント](https://git-scm.com/doc)
- [GitHub Docs](https://docs.github.com)
- [GitHub CLI](https://cli.github.com/manual/)

### 視覚的な学習

- [Learn Git Branching](https://learngitbranching.js.org/) - インタラクティブなGit学習
- [Visualizing Git](https://git-school.github.io/visualizing-git/) - コマンドの可視化

### チートシート

- [GitHub Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

---

## まとめ

### Gitの本質

```
変更を記録 → 履歴を保持 → チームで共有
```

### 重要な概念

1. **4つのエリア**: ワークツリー → ステージング → ローカル → リモート
2. **ブランチ**: masterを守りながら開発
3. **プルリクエスト**: レビューを経てマージ
4. **コンフリクト**: 恐れず、落ち着いて解決

### 毎日使うコマンド

```bash
# 朝
git checkout master
git pull origin master
git checkout -b feature/new-task

# 作業中
git add .
git commit -m "作業内容"
git push origin feature/new-task

# 夕方
gh pr create
```

### 困ったら

1. `git status` で状態確認
2. `git log --graph` で履歴確認
3. わからなければチームに相談

**Git is not scary. Git is your friend!** 🚀

---

## このガイドについて

- **作成日**: 2026-01-06
- **対象**: Git初心者～中級者
- **前提知識**: 基本的なコマンドライン操作

### 実践リポジトリ

このガイドの内容は以下のリポジトリで実際に体験できます：

- GitHub: https://github.com/kojimak-collab/git-collaboration-test
- ローカル: `C:\dev\Obsidian-Vault\git-collaboration-test`

### フィードバック

改善点や質問があれば、Issueやプルリクエストでお知らせください！

---

**Happy Coding with Git!** 🎉
